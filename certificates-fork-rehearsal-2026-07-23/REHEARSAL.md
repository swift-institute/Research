# apple/swift-certificates Single-Repo True Fork (N5) — Rehearsal-Inventory Record (2026-07-23)

```
<!--
---
version: 1.0.0
last_updated: 2026-07-23
status: EVIDENCE
tier: 2
scope: Institute swift-certificates publication mechanics + reshape inventory (N5)
---
-->
```

Analog of `Research/asn1-split-rehearsal-2026-07-23/REHEARSAL.md`, for the fork authorized by `certificates-n5-decision-packet.md` §DECISION/§EXECUTION LOG item 4. Temp-clone only (scratchpad `cert-inventory/`); no repo mutated; no build run.

## FACTS — Fork point verified

- Clone `--branch 1.18.0` of `apple/swift-certificates`: HEAD = `24ccdeeeed4dfaae7955fcac9dbf5489ed4f1a25` = tag `1.18.0` (lightweight tag, `cat-file -t` = commit), "Custom private key (#282)", 2026-02-10; **312 commits reachable**; commit **is an ancestor of `origin/main`** (merge-base probe PASS). Matches the packet's fork-point ruling exactly (`certificates-n5-decision-packet.md:349`).
- Layout: one product `X509` from targets `X509` + `_CertificateInternals` (`Package.swift:22–:24`, `:28–:39`, `:58–:63`); deps `swift-crypto "3.12.3"..<"5.0.0"` and `swift-asn1 from: "1.1.0"` (`Package.swift:79–:81`) — note the asn1 floor predates the BER APIs 1.18.0 actually uses (`CMSContentInfo.swift` `BERImplicitlyTaggable`, introduced in asn1 1.6.x); resolution floats to 1.7.1 per the packet's clean-room log (`certificates-n5-decision-packet.md:308–:310`). Manifest itself imports Foundation (`Package.swift:17` `import class Foundation.ProcessInfo`).
- 109 source files (`Sources/X509` 108 + `_CertificateInternals/_TinyArray.swift`), 24 test files, 30 non-Swift test resources.

## 1. Path partition (proposal, anchored to the record)

Governing cut: "The package's sole L3 essence is certificate verification. RFC 5280 wire/profile law moves to the independent L2 RFC owner; system-root acquisition moves to dedicated integration packages" (record `:350`–`:352`); [HERITAGE-001] material lineage names "chain construction, signature verification, and policy lineage" (record `:344`).

**STAYS — L3 verifier essence (publication tree):**

- Chain construction: `Sources/X509/Verifier/Verifier.swift` (`Verifier`, `CandidatePartialChain`, `VerificationResult`, `CertificateValidationResult`), `UnverifiedChain.swift`, `ValidatedCertificateChain.swift`, `VerificationDiagnostic.swift`.
- Policy engine: `Verifier/VerifierPolicy.swift`, `PolicyBuilder.swift`, `AnyPolicy.swift`, `AllOfPolicies.swift`, `OneOfPolicies.swift`.
- Anchor-store abstraction: `Verifier/CertificateStore.swift`, `CustomCertificateStore.swift` — MINUS the `systemTrustStore` plumbing (`CertificateStore.swift:44–:45`, `:85`, `:96–:97`, `:164–:178`, `:193`, `:210`), which is the system-trust seam the integrations fill.
- RFC 5280 policy lineage (the "policy lineage" of `:344`): `Verifier/RFC5280/{RFC5280Policy,ExpiryPolicy,BasicConstraintsPolicy,VersionPolicy,NameConstraintsPolicy,DNSNames,DirectoryNames,IPConstraints,URIConstraints}.swift` (9 files).
- TLS server identity: `Verifier/ServerIdentityPolicy.swift` (hostname/SAN/IP matching; CN fallback present upstream — the record's stricter WebPKI policy is a reshape addition, record `:462`–`:464`, `:475` item 9).
- Certificate model + signature-verify surfaces the verifier binds: `Certificate.swift` (minus exclusions below), `CertificateSerialNumber.swift`, `CertificateVersion.swift`, `CertificatePublicKey.swift` (verify half), `Signature.swift` (verify half), `SignatureAlgorithm.swift`, `Digests.swift`, `X509BaseTypes/{TBSCertificate,AlgorithmIdentifier,SubjectPublicKeyInfo,ECDSASignature,Time,TimeCalculations,Validity}.swift`, `DistinguishedName.swift`, `RelativeDistinguishedName.swift`, `RDNAttribute.swift`, `GeneralName.swift`, `Extension.swift`, `Extensions.swift`, `Extension Types/` (10 files), `Error.swift`, `X509SendableMetatype.swift`, `_CertificateInternals/_TinyArray.swift` (+ its test).
- Both: `LICENSE.txt`, `NOTICE.txt`, `CONTRIBUTORS.txt` (attribution per [HERITAGE-002] acceptance artifacts, packet checklist item 5).

**EXCLUDED — RFC 5280 wire/profile law (responsibility transfer, NOT a file transplant).** Hard constraint: "Do not move Apple certificate-runtime source into this L2 owner; verifier lineage stays solely in the true `swift-certificates` fork" (record `:307`). So these files stay in the fork's history/tree, but their LAW is the L2 `swift-rfc-5280` owner's mission (record `:161`) and must not be duplicated at reshape (record `:484` item 6). Profile-law surfaces, exactly: `TBSCertificate.swift` (wire structure), `Extension Types/{ExtensionIdentifiers,BasicConstraints,KeyUsage,ExtendedKeyUsage,SubjectAlternativeName,NameConstraints,AuthorityKeyIdentifier,SubjectKeyIdentifier,AuthorityInformationAccess}.swift` (extension wire + criticality semantics), `DistinguishedName.swift`/`RelativeDistinguishedName.swift`/`RDNAttribute.swift` + `OCSP/DirectoryString.swift` (misfiled under OCSP/ upstream — RFC 5280 §4.1.2.4 DirectoryString), `GeneralName.swift`, `X509BaseTypes/{Time,TimeCalculations,Validity}.swift` (UTCTime/GeneralizedTime cutover law), `AlgorithmIdentifier.swift` (OID/params law), `SubjectPublicKeyInfo.swift`, `CertificateVersion.swift`/`CertificateSerialNumber.swift`, and the pure name-matching halves `RFC5280/{DNSNames,IPConstraints,URIConstraints}.swift`. **Local status:** no `swift-rfc-5280` checkout exists anywhere under `/Users/coen/Developer` (glob + `swift-standards` listing both empty) — it is a remote reservation only (record `:307`); an Institute-authored L2 tree is a prerequisite lane for the no-duplication reshape.
- **System-root acquisition → integration packages**: `Verifier/TrustRootLoading.swift` entire file (search paths `:26–:29`; `systemTrustRoots` `:43–:47`; Dispatch-backed cache `:49–:56`; `loadTrustRoots` `:61`; PEM parse `:87`) → `swift-certificates-linux`; the `SecCertificate` bridge `Certificate.swift:438–:466` (`import Security` at `:439`) and `SecKeyWrapper.swift` (whole file; `@preconcurrency import Security` `:24`) → `swift-certificates-darwin-standard`. The concurrency shims `Lock.swift`, `LockedValueBox.swift`, `PromiseAndFuture.swift` are used ONLY by this plumbing (usage probe: `Future`/`Promise`/`Lock` appear outside their own files only in `TrustRootLoading.swift`; the `ServerIdentityPolicy.swift:458` hit is prose in a comment) → excluded with it.
- **PEM/Foundation surfaces**: `Certificate.swift:433–:436` (`PEMRepresentable`), `CertificatePublicKey.swift:389–:392`, `CSR/CertificateSigningRequest.swift:298–:301`, `CertificatePrivateKey.swift:340–:395`, `Curve25519+DER.swift:47–:48`, `SEC1PrivateKey.swift:40–:42`, `CustomPrivateKey.swift:24` (protocol requires `PEMSerializable`), `SecKeyWrapper.swift:290–:331`.
- **CryptoExtras/RSA (out of slice 1**, record `:485`–`:487`, packet "What is NOT being asked"): `import _CryptoExtras` at `CertificatePrivateKey.swift:22`, `CertificatePublicKey.swift:17`, `SecKeyWrapper.swift:23`, `Signature.swift:17`; `_RSA` arms (9 uses `Signature.swift`, 4 each in `CertificatePublicKey.swift`/`CertificatePrivateKey.swift`, 2 `SecKeyWrapper.swift`); `X509BaseTypes/RSAPKCS1PublicKey.swift` (pure-ASN.1 wire type, but RSA-gated); test resource `PEMTestRSACertificate.pem`.
- **Not verifier essence (straddlers, proposed EXCLUDED from slice-1 tree — see Open Questions)**: issuance side — `CertificatePrivateKey.swift`, `CustomPrivateKey.swift`, `PKCS8PrivateKey.swift`, `SEC1PrivateKey.swift`, `Curve25519+DER.swift`, `RandomNumberGenerator+bytes.swift`, `CSR/` (6 files), `DistinguishedNameBuilder/` (10 files), `ExtensionsBuilder.swift`; `CryptographicMessageSyntax/` (10 files, CMS sign/verify — not TLS server verification); `OCSP/` (18 files; revocation — absent from the N5 acceptance list, record `:795`–`:799`).
- **Docs/CMake/CI/meta** (ASN.1-rehearsal precedent, REHEARSAL.md:44–:45): `Sources/X509/Docs.docc/`, `CMakeLists.txt` + `cmake/`, `Benchmarks/`, `dev/`, `.github/`, `Package.swift` (replaced), README/meta dotfiles (`.editorconfig`, `.licenseignore`, `.spi.yml`, `.swift-format`).

## 2. FACTS — Foundation / platform-C import census (every site, file:line)

Pattern is `#if canImport(FoundationEssentials) / #else import Foundation` pairs; both lines listed.

**Foundation/FoundationEssentials (23 files):** `CSR/CertificateSigningRequest.swift:16,:18`; `Certificate.swift:16,:18`; `CertificatePrivateKey.swift:17,:19`; `CertificatePublicKey.swift:19,:21`; `CryptographicMessageSyntax/CMSOperations.swift:15,:17`; `CMSSignature.swift:17,:19`; `CMSSignerInfo.swift:16,:18`; `Curve25519+DER.swift:16,:18`; `CustomPrivateKey.swift:16,:18`; `Digests.swift:16,:18`; `Extension Types/SubjectKeyIdentifier.swift:17` (`import struct Foundation.Data`); `OCSP/OCSPPolicy.swift:18,:20`; `SecKeyWrapper.swift:18,:20`; `Signature.swift:19,:21`; `Verifier/RFC5280/BasicConstraintsPolicy.swift:15,:17`; `ExpiryPolicy.swift:16,:18`; `NameConstraintsPolicy.swift:15,:17`; `RFC5280Policy.swift:15,:17`; `URIConstraints.swift:16,:18`; `Verifier/ServerIdentityPolicy.swift:16,:18`; `Verifier/TrustRootLoading.swift:16,:19`; `X509BaseTypes/ECDSASignature.swift:15,:17`; `X509BaseTypes/Time.swift:16,:18`. What they use: `Date` (verifier-critical — `ExpiryPolicy` 4, `RFC5280Policy` 3, `Certificate` 10, `Time.swift` 6, `OCSPPolicy` 13, CMS 7) and `Data` (`Signature` 3, `ECDSASignature` 7, `SecKeyWrapper` 7, `TrustRootLoading` 3, `CMSOperations` 2, `SubjectKeyIdentifier` 1). Several importers use neither directly (e.g. `CertificateSigningRequest`, `CertificatePublicKey`, `Digests`, `BasicConstraintsPolicy`, `NameConstraintsPolicy`, `URIConstraints`, `ServerIdentityPolicy` show Data=0/Date=0) — conditional-compilation boilerplate, trivially strippable.

**Platform C / system frameworks (Sources):** `Lock.swift:16` Darwin, `:18` ucrt, `:19` WinSDK, `:21` Glibc, `:23` Musl, `:25` Bionic, `:27` WASILibc, `:29` wasi_pthread; `Verifier/ServerIdentityPolicy.swift:22` WinSDK, `:24` Android, `:26` Glibc, `:28` Musl (for `inet_pton` at `:162`, `:175`); `Verifier/RFC5280/URIConstraints.swift:21` WinSDK, `:23` Glibc, `:24,:27,:32` CoreFoundation, `:26` Musl, `:29` Darwin, `:31` Android (for `inet_pton` at `:90`); `Certificate.swift:439` Security (inside `#if canImport(Security)` `:438`); `SecKeyWrapper.swift:24` `@preconcurrency import Security`; `Verifier/TrustRootLoading.swift:17` Dispatch; `OCSP/OCSPPolicy.swift:24` `_Concurrency` (benign). Tests: `CertificateDERTests.swift:27` Security, `SecKeyWrapperTests.swift:18` Security, `SignatureTests.swift:21` Security, `IPAddressTests.swift:19` Android. This confirms and completes the record's claim at `:459` ("X509 has Foundation/FoundationEssentials and direct Darwin/Glibc/Musl imports"). Note the two verifier-essence platform-C sites (`ServerIdentityPolicy`, `URIConstraints`) exist solely for IP-literal parsing — Institute `swift-ipv4-standard`/`swift-ipv6-standard` exist as replacements (`/Users/coen/Developer/swift-standards/`).

## 3. FACTS — Crypto product boundary (→ future `swift-certificates-crypto` adapter, record `:163`, `:472` item 2)

Import sites: `CertificatePrivateKey.swift:21` (`@preconcurrency import Crypto`) + `:22` `_CryptoExtras`; `CertificatePublicKey.swift:16` + `:17`; `CMSOperations.swift:20`; `Curve25519+DER.swift:21`; `Digests.swift:20`; `Extension Types/SubjectKeyIdentifier.swift:16`; `OCSP/OCSPPolicy.swift:16`; `SecKeyWrapper.swift:22–:23`; `Signature.swift:16–:17`; `X509BaseTypes/ECDSASignature.swift:20`. Tests additionally import Crypto (`Package.swift:44`).

Primitives per file (token census): **signature verify/key kinds** — `CertificatePublicKey.swift` P256/P384/P521 (9 each), Curve25519/Ed25519 (6), `_RSA` (4); `Signature.swift` P256 (8), P384/P521 (6), Curve25519 (2), `_RSA` (9), digest selection SHA256/384/512 (9 each) + `Insecure.SHA1` (2); `ECDSASignature.swift` P256/P384/P521 raw-signature bridging (2 each); `SignatureAlgorithm.swift` OID↔algorithm mapping (SHA256/384/512 ×2, P256/P384/P521/Curve25519 ×1). **Hashing** — `Digests.swift` (SHA256/384/512, `Insecure.SHA1`, generic `Digest`); `SubjectKeyIdentifier.swift` `Insecure.SHA1` (SKI computation); `OCSPPolicy.swift` `Insecure.SHA1` (2) + SHA256 (CertID). **Issuance-side (out of essence)** — `CertificatePrivateKey.swift` P256 (12), P384/P521 (7), Curve25519 (3), `_RSA` (4); `SecKeyWrapper.swift` P256/P384/P521 (8 each), `_RSA` (2). Slice-1 adapter surface therefore needs: ECDSA P256/P384/P521 verify, Ed25519 verify, SHA-256/384/512 + SHA-1 (legacy digest + SKI), and nothing RSA until the CryptoExtras gate opens.

## 4. FACTS — SwiftASN1 dependency surface vs the swift-iso-8824/8825 trees

`import SwiftASN1` appears in 87 source files (every non-excluded file except `Lock/LockedValueBox/PromiseAndFuture/Digests/RandomNumberGenerator+bytes/_TinyArray/Error.swift` and builders). API census (Sources, occurrence counts): `DER.Serializer` 95, `DER.sequence` 53, `DER.optionalExplicitlyTagged` 19, `DER.optionalImplicitlyTagged` 11, `DER.set` 10, `DER.decodeDefaultExplicitlyTagged` 3, `DER.explicitlyTagged` 2, `DER.decodeDefault` 2, `DER.lazySet`/`DER.LazySetOfSequence` 1 each; `BER.sequence` 5, `BER.set` 4, `BER.optionalImplicitlyTagged` 3, `BER.explicitlyTagged`/`optionalExplicitlyTagged` 1 each; types: `ASN1ObjectIdentifier` 171, `ASN1Identifier` 166, `ASN1OctetString` 67, `ASN1Node` 63, `DERImplicitlyTaggable` 49, `ASN1Any` 46, `GeneralizedTime` 34, `ASN1Error` 34, `ASN1BitString` 33, `ASN1Null` 13, `PEMDocument` 13, `UTCTime` 10, `DERSerializable` 9, `DERParseable` 8, `BERImplicitlyTaggable` 7, `PEMRepresentable` 3, `PEMSerializable` 2, `IntegerBytesCollection` 2, `ASN1NodeCollection` 2, `PEMParseable`/`BERParseable`/`BERSerializable` 1 each.

**Mapping against `/Users/coen/Developer/swift-iso/` (read late-run; the 8824/8825 trees appeared during this session — they were absent from my opening listing, consistent with parallel authoring):** both repos sit at their filtered heads (`swift-iso-8824` @ 41343ec, `swift-iso-8825` @ 5195023 — matching `commit-map-8824.txt`/`commit-map-8825.txt`), pre-reshape, still SwiftASN1 spellings, no `Package.swift` yet. Presence probes (positive-controlled by the hits themselves): `DER.Serializer`/`sequence`/`set`/`lazySet`/`LazySetOfSequence`/`decodeDefault*`/`optional*Tagged` + BER equivalents → `swift-iso-8825/Sources/ISO 8825/{DER,BER}.swift`; `ASN1Node`/`ASN1NodeCollection` → `ASN1.swift`; `ASN1Any`, `BERParseable`/`BERSerializable` → `ASN1Any.swift`/`ArraySliceBigint.swift`; `DERImplicitlyTaggable` → `DER.swift`; `ASN1ObjectIdentifier` → `swift-iso-8824/Sources/ISO 8824/ObjectIdentifier.swift`; `GeneralizedTime`/`UTCTime`/`ASN1BitString`/`ASN1OctetString`/`ASN1Null`/`ASN1IntegerRepresentable`/`IntegerBytesCollection`/`ASN1Error` → 8824 files; `BERImplicitlyTaggable` protocol reachable (conformances across both trees).

**Gaps for the 8824/8825 lanes:**
1. `PEMDocument` / `PEMDocument.parseMultiple` (`TrustRootLoading.swift:87`) and `PEMDocument(pemString:)`/`(type:derBytes:)` sites — PEM was EXCLUDED from both 882x trees (ASN.1 rehearsal "Excluded", REHEARSAL.md:44). Not a slice-1 gap for the fork essence (all PEM users are themselves excluded surfaces), but `swift-certificates-linux` (trust-root bundles are PEM) and any PEM convenience NEED an Institute PEM owner — currently homeless. Flag to the lead.
2. `ASN1Error.invalidPEMDocument` (`CertificatePrivateKey.swift:357`) — the error case survives in `8824/Errors.swift` but is PEM-referencing; reshape of Errors.swift may drop it, only excluded surfaces use it.
3. Version-float risk: certificates 1.18.0 resolves asn1 at 1.7.1 (packet execution log), while the 882x fork point is 1.6.0. Every API the census found exists in the 1.6.0-derived trees (probe above), so **no 1.7.x-only API is used** — but this must be re-proven after any upstream bump.

## 5. Straddler rulings + heritage mechanics (ANALYSIS / RECOMMENDATION)

**Single-repo true fork SUFFICES — CONFIRMED.** One upstream → one Institute repo (`swift-certificates`, name vacated per packet EXECUTION LOG item 2). No path-filter split is needed: unlike swift-asn1, no second Institute owner receives upstream files — the `swift-rfc-5280` transfer is a responsibility transfer with an explicit no-source-transplant rule (record `:307`), and the Darwin/Linux/system integrations are NEW Institute-authored packages (record `:164`–`:166`), not history recipients. Therefore [HERITAGE-002] applies in its direct fork-badge shape: GitHub-native fork of `apple/swift-certificates`, one publication commit whose **parent = `24ccdee…`** and whose **tree = publication state with all exclusions applied as tree deletions in that commit** (recorded as "deleted upstream surfaces" acceptance artifacts, packet checklist item 5).

**Control #1 analog (fork-point survival): PASS, and trivially so.** In the ASN.1 rehearsal the filters PRUNED the fork-point commit (REHEARSAL.md:52–:57); here no filtering occurs, so `24ccdee` survives literally as the publication commit's parent — the [HERITAGE-002] parent-pointer holds without the message-names-fork-point workaround. Verified: `24ccdee` = tag target, ancestor of `origin/main`, 312-commit history intact.

**Control #2 analog (ref/tag leakage): CONFIRMED PRESENT — real run must compensate.** Even `clone --branch 1.18.0` fetched all remote branches (`origin/main`, `origin/release/1.10.x`, `origin/dn-package-benchmark`, `origin/dn-update-benchmarks`, `origin/swift-crypto-5.x`) and **37 upstream tags**. Real run: fetch the single reviewed ref `--no-tags` into the Institute repo; upstream tags are not the Institute release line ([HERITAGE-004], per ASN.1 rehearsal item 78).

**Controls #3/#4 analogs:** no filter-repo → no rename-following or commit-map concerns; no `origin` stripping. Standard rules still apply: never force-push; import = fetch + reviewed-tree publication commit; no orphan/squash/mirror-sync shapes ([HERITAGE-002] risks, packet `:204`–`:206`).

**Straddler rulings proposed (need lead/principal sign-off):**
- (a) `Verifier/RFC5280/` policy files: STAY (they ARE the adjudicated "policy lineage", record `:344`); their pure matching-law halves (`DNSNames`, `IPConstraints`, `URIConstraints`) are future L2 law, to be superseded by `swift-rfc-5280` WITHOUT moving the files there.
- (b) Issuance side (private keys, CSR, DN/extension builders), CMS, OCSP: propose EXCLUDED from the slice-1 publication tree (sole essence = verification, record `:350`) — BUT see Open Question 2: the upstream verifier test suite generates its certificate fixtures in-test via issuance APIs (`VerifierTests.swift` 25 hits, `RFC5280PolicyTests.swift` 10, `ServerIdentityPolicyTests.swift` 6 for `Certificate.PrivateKey`/`P256.Signing`), so full issuance exclusion forces either a pre-generated DER fixture corpus or an internal/test-only issuance retention.
- (c) `CertificateStore` system-trust plumbing + `Lock`/`LockedValueBox`/`PromiseAndFuture`: excluded with `TrustRootLoading.swift`; the `CertificateStore` seam that remains must be redesigned as the injected system-trust witness (GATE A shape, packet `:67`–`:71`).

## 6. Reshape inventory (inventory, not design)

- **Compound names → Nest.Name**: 116 top-level type declarations in Sources; upstream already nests some (`Certificate.PublicKey`, `Certificate.PrivateKey`, `Certificate.Extensions` via extensions) but the compound offenders include `CertificateError`, `CertificateStore`, `CustomCertificateStore`, `UnverifiedCertificateChain`, `ValidatedCertificateChain`, `VerificationDiagnostic`, `VerifierPolicy`, `PolicyEvaluationResult`, `PolicyFailureReason`, `RFC5280Policy`, `ServerIdentityPolicy`, `AllOfPolicies`/`OneOfPolicies`/`AnyPolicy`, `DistinguishedName`, `RelativeDistinguishedName`, `GeneralName`, `TBSCertificate`, `AlgorithmIdentifier`, `SubjectPublicKeyInfo`, `ECDSASignature`, `RSAPKCS1PublicKey`, the 11 `CMS*` and 18 `OCSP*` types, `CertificateSigningRequest` family. Spec-mirroring rule applies (`RFC_5280.*`-style law names live in L2; L3 keeps runtime names).
- **Typed throws**: 283 untyped `throws` sites in Sources, **0** typed — full conversion per record `:475` item 3 and the N5 STOP "missing typed errors" (`:744`). Error currencies today: `CertificateError` (`Error.swift:27`) + upstream `ASN1Error` (34 uses).
- **Byte surfaces**: 77 `[UInt8]`/`ArraySlice<UInt8>` occurrences in Sources, 64 in Tests → Byte/Span discipline at the boundary (record `:474` item 3/7).
- **Foundation strip**: 23 importer files (census §2); semantic load is `Date` (verifier: expiry/validity — needs the Institute Time primitive) and `Data` (byte-bridging, mechanical). Platform-C strip: `Lock.swift` (8 imports, excluded with plumbing), `inet_pton` ×2 files (replace with swift-ipv4/ipv6-standard parsing), Security ×2 files (→ Darwin integration), Dispatch ×1 (excluded).
- **XCTest → swift-testing**: 23 of 24 test files import XCTest; 1 (`Tests/X509Tests/CustomPrivateKeyTests.swift`) already imports Testing; **584 `func test` cases**. Roughly 6× the ASN.1 conversion (4 files).
- **Fixture corpus vs N5 gates** (record `:795`–:799: roots/intermediates, expiry/not-yet-valid, signature mismatch, constraints/path-length, unknown critical extension, keyUsage/serverAuth EKU, SAN DNS/IP, encoding/NUL rejection, IDNA, wildcard boundaries, CN-fallback rejection): upstream ships 30 non-Swift resources — 5 OCSP DER files (`www.apple.com` chain + 2 OCSP responses), `PEMTestRSACertificate.pem`, `CSR Vectors/cryptography/`, `ca-certificates.crt` (`Package.swift:47–:55`). The chain/hostname/policy gates are covered upstream by in-test GENERATED certificates (`VerifierTests`, `RFC5280PolicyTests`, `ServerIdentityPolicyTests`, `NameConstraintsTests`, `DNSNamesTests`, `IPAddressTests`), not by static fixtures — the N5 fixture corpus must be either regenerated through retained issuance code or frozen to DER once and committed. CN-fallback upstream is accept-leaning (`ServerIdentityPolicy`); the Institute gate requires rejection — new fixtures needed. IDNA fixtures: none found upstream — gap.

## Open questions for the principal / lead

1. **Slice-1 tree scope**: confirm exclusion of CSR/CMS/OCSP/issuance/builders from the publication tree, or retain-with-flag (history keeps them either way; this only sets the publication commit's deletions list).
2. **Fixture strategy** forced by (1): pre-generated committed DER chain corpus vs internal test-only issuance retention (`Certificate.PrivateKey` et al.).
3. **PEM owner**: Institute home for PEM (needed by `swift-certificates-linux` trust bundles) — excluded from 8824/8825 and from this fork; currently unassigned.
4. **`Date`/time primitive**: which Institute time surface replaces `Foundation.Date` in `ExpiryPolicy`/`Validity` (record `:215` "Byte/Time primitives") — selection not yet pinned.
5. **`swift-rfc-5280`**: no local checkout exists; the improve-independently lane (record `:161`, `:307`) has no visible tree yet — sequencing risk for the no-duplication reshape.

Hands-off edges encountered: none (no `coenttb/*`, `rule-*`, or repotraffic paths touched). Scratchpad artifacts: clone at `/private/tmp/claude-501/-Users-coen-Developer/af9e0509-6914-41ac-badd-612493c10c96/scratchpad/cert-inventory/swift-certificates`. Key referenced files: `/Users/coen/Developer/swift-institute/Research/certificates-n5-decision-packet.md`, `/Users/coen/Developer/swift-institute/Research/native-networking-wave-3-implementation-heritage-dependency-record.md`, `/Users/coen/Developer/swift-institute/Research/asn1-split-rehearsal-2026-07-23/REHEARSAL.md`, `/Users/coen/Developer/swift-iso/swift-iso-8824`, `/Users/coen/Developer/swift-iso/swift-iso-8825`.