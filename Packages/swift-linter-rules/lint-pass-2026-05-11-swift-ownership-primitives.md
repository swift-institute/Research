# Lint pass: swift-ownership-primitives — 2026-05-11

**Command**: `swift-linter --all /Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources`
**Tool**: swift-linter @ 17a977715520af9f4f8c1d047da778e9583fb462
**Rules**: all 73 from 13 packs

## Raw findings

```
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Borrow Primitives/Ownership.Borrow.swift:103:18: warning: inlinable_internal_access: [inlinable_internal_access] [PATTERN-052]: `@inlinable` cross-module access requires `@usableFromInline` (or `public` / `package`), not bare `internal`. An `@inlinable` decl whose body references an `internal` identifier fails to compile across the module boundary; pair the attribute with `@usableFromInline package` (preferred for impl-only surface) or upgrade to `public`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Borrow Primitives/Ownership.Borrow.swift:197:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Borrow Primitives/Ownership.Borrow.swift:117:16: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Borrow Primitives/Tagged+Ownership.Borrow.Protocol.swift:31:12: warning: namespace_adoption_typealias: [namespace_adoption_typealias] [API-NAME-004a]: same-leaf typealias is the namespace-adoption shape. Confirm the higher-layer namespace declares ≥ 5 sibling types / extensions / methods on the adopted concept — otherwise this is a rename bridge per [API-NAME-004]. Surfaced as a review prompt.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Borrow Primitives/Tagged+Ownership.Borrow.Protocol.swift:31:22: warning: typealiased_namespace_bridge: [typealiased_namespace_bridge] [PLAT-ARCH-018]: typealias whose LHS name matches its RHS member-type leaf silently bridges a foreign namespace into the local one. New-type declarations at `<local>.<aliased>.<NewName>` resolve to the foreign module — any existing type at the same foreign path conflicts silently. Before adding sub-types via this aliased path, grep the foreign module for collisions and choose a non-conflicting sub-path, a non-typealiased namespace entry, or relocate the foreign type.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Borrow Primitives/_Ownership_Borrow_OwnedBuffer.swift:32:5: warning: inlinable_internal_access: [inlinable_internal_access] [PATTERN-052]: `@inlinable` cross-module access requires `@usableFromInline` (or `public` / `package`), not bare `internal`. An `@inlinable` decl whose body references an `internal` identifier fails to compile across the module boundary; pair the attribute with `@usableFromInline package` (preferred for impl-only surface) or upgrade to `public`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Indirect Primitives/Ownership.Indirect.swift:73:21: warning: ad_hoc_box_class: [ad_hoc_box_class] [IMPL-107]: ad-hoc `_Box` / `_Storage` reference wrapper duplicates ecosystem primitives. Prefer `Reference<T>` (shared mutable indirection) or `Owned<T>` (unique-owner indirection) from `swift-ownership-primitives` so the wrapper's ownership story is checked by the type system, not ad-hoc.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Indirect Primitives/Ownership.Indirect.swift:15:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Indirect Primitives/Ownership.Indirect.swift:145:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Indirect Primitives/Ownership.Indirect.swift:73:15: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:198:21: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:31:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:23:30: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:24:30: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:25:30: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:26:30: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:102:9: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:137:16: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:166:16: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:198:16: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Latch Primitives/Ownership.Latch.swift:224:16: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Mutable Primitives/Ownership.Mutable.swift:13:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Primitives/Ownership Primitives.docc/Resources/step-06-inout.swift:8:6: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Shared Primitives/Ownership.Shared.swift:13:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Slot Primitives/Ownership.Slot.Move.swift:51:19: warning: unchecked_call_site: [unchecked_call_site] [CONV-016]: `__unchecked:` at a call site is a Tier-5 last-resort bypass of the typed system. Prefer `.retag()` (Tier 1) or `.map()` (Tier 2) before resorting to `__unchecked:`. If this site is the typed-system bottom-out (extension-init internals, [CONV-001] permitted same-package use), escalate to supervisor and apply `// swiftlint:disable:next unchecked_call_site  // reason: <citation>`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Slot Primitives/Ownership.Slot.Move.swift:59:20: warning: unchecked_call_site: [unchecked_call_site] [CONV-016]: `__unchecked:` at a call site is a Tier-5 last-resort bypass of the typed system. Prefer `.retag()` (Tier 1) or `.map()` (Tier 2) before resorting to `__unchecked:`. If this site is the typed-system bottom-out (extension-init internals, [CONV-001] permitted same-package use), escalate to supervisor and apply `// swiftlint:disable:next unchecked_call_site  // reason: <citation>`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Slot Primitives/Ownership.Slot.swift:30:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Slot Primitives/Ownership.Slot.swift:23:30: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Slot Primitives/Ownership.Slot.swift:24:30: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Slot Primitives/Ownership.Slot.swift:25:30: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Slot Primitives/Ownership.Slot.swift:115:9: warning: minimal_type_body: [minimal_type_body] [API-IMPL-008]: type bodies MUST contain ONLY stored properties, the canonical initializer(s), and (for classes / ~Copyable types) `deinit`. Methods, computed properties, static members, nested types, and protocol conformances belong in extensions. Minimal bodies make storage layout immediately visible and separate stable data from evolving behavior.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Incoming.swift:82:27: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Incoming.swift:45:40: warning: unnecessary_unchecked_sendable_noncopyable: [unnecessary_unchecked_sendable_noncopyable] [MEM-SEND-004]: `~Copyable` structs whose stored properties are all `Sendable` MUST use plain `Sendable`. The compiler synthesises and checks `Sendable` for `~Copyable` structs the same way as for `Copyable` ones — there is no inference gap. `@unchecked Sendable` here is a misleading safety claim. Drop `@unchecked` and let the checker verify.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Incoming.swift:57:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:61:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:64:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:107:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:108:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:109:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:110:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:113:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:114:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:123:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:135:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:149:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:151:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Erased Primitives/Ownership.Transfer.Erased.Outgoing.swift:172:13: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Primitives/Ownership.Transfer.Retained.Incoming.swift:96:27: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Primitives/Ownership.Transfer.Retained.Incoming.swift:56:40: warning: unnecessary_unchecked_sendable_noncopyable: [unnecessary_unchecked_sendable_noncopyable] [MEM-SEND-004]: `~Copyable` structs whose stored properties are all `Sendable` MUST use plain `Sendable`. The compiler synthesises and checks `Sendable` for `~Copyable` structs the same way as for `Copyable` ones — there is no inference gap. `@unchecked Sendable` here is a misleading safety claim. Drop `@unchecked` and let the checker verify.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Primitives/Ownership.Transfer.Retained.Incoming.swift:68:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Primitives/Ownership.Transfer.Retained.Outgoing.swift:55:40: warning: unnecessary_unchecked_sendable_noncopyable: [unnecessary_unchecked_sendable_noncopyable] [MEM-SEND-004]: `~Copyable` structs whose stored properties are all `Sendable` MUST use plain `Sendable`. The compiler synthesises and checks `Sendable` for `~Copyable` structs the same way as for `Copyable` ones — there is no inference gap. `@unchecked Sendable` here is a misleading safety claim. Drop `@unchecked` and let the checker verify.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Primitives/Ownership.Transfer.Retained.Outgoing.swift:100:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Transfer Primitives/Ownership.Transfer.Value.Incoming.swift:87:27: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Unique Primitives/Ownership.Unique+Copyable.swift:15:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Unique Primitives/Ownership.Unique.swift:167:16: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-ownership-primitives/Sources/Ownership Unique Primitives/Ownership.Unique.swift:15:11: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
```
