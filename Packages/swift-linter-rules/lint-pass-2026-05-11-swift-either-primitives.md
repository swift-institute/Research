# Lint pass: swift-either-primitives — 2026-05-11

**Command**: `swift-linter --all /Users/coen/Developer/swift-primitives/swift-either-primitives/Sources`
**Tool**: swift-linter @ 17a977715520af9f4f8c1d047da778e9583fb462
**Rules**: all 73 from 13 packs

## Raw findings

```
/Users/coen/Developer/swift-primitives/swift-either-primitives/Sources/Either Primitives/Either+FlatMap.swift:32:24: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-either-primitives/Sources/Either Primitives/Either+FlatMap.swift:47:24: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-either-primitives/Sources/Either Primitives/Either+FlatMap.swift:75:24: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-either-primitives/Sources/Either Primitives/Either+FlatMap.swift:95:27: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-either-primitives/Sources/Either Primitives/Either+FlatMap.swift:103:27: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-either-primitives/Sources/Either Primitives/Either+FlatMap.swift:117:27: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
```
