# Lint pass: swift-equation-primitives — 2026-05-11

**Command**: `swift-linter --all /Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources`
**Tool**: swift-linter @ 17a977715520af9f4f8c1d047da778e9583fb462
**Rules**: all 73 from 13 packs

## Raw findings

```
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Core/Equation.Protocol.swift:18:16: warning: unification_bridge_typealias: [unification_bridge_typealias] [API-NAME-004]: typealias renames a member type to a different local name. Type unification MUST use the canonical type at all call sites; a typealias bridge adds indirection without domain value.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Core/Equation.Protocol.swift:23:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Array.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.ArraySlice.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.CollectionOfOne.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.ContiguousArray.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Dictionary.swift:24:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Dictionary.swift:25:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Dictionary.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.EmptyCollection.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.KeyValuePairs.swift:20:38: warning: borrowing_self_short_circuit: [borrowing_self_short_circuit] [IMPL-094]: operator overload with `borrowing Self` parameters uses `&&` / `||` in its body — Swift 6.3 rejects chained property access across the short-circuit boundary as `borrowed value escapes its borrow scope`. Use tuple comparison `(lhs.a, lhs.b) < (rhs.a, rhs.b)` or local `let` bindings materialising the values before the short-circuit.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.KeyValuePairs.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Optional.swift:20:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Optional.swift:21:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Optional.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.PartialRange.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.PartialRange.swift:19:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.PartialRange.swift:33:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Range.swift:19:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Range.swift:20:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Range.swift:39:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Range.swift:40:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Range.swift:21:61: warning: borrowing_self_short_circuit: [borrowing_self_short_circuit] [IMPL-094]: operator overload with `borrowing Self` parameters uses `&&` / `||` in its body — Swift 6.3 rejects chained property access across the short-circuit boundary as `borrowed value escapes its borrow scope`. Use tuple comparison `(lhs.a, lhs.b) < (rhs.a, rhs.b)` or local `let` bindings materialising the values before the short-circuit.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Range.swift:41:61: warning: borrowing_self_short_circuit: [borrowing_self_short_circuit] [IMPL-094]: operator overload with `borrowing Self` parameters uses `&&` / `||` in its body — Swift 6.3 rejects chained property access across the short-circuit boundary as `borrowed value escapes its borrow scope`. Use tuple comparison `(lhs.a, lhs.b) < (rhs.a, rhs.b)` or local `let` bindings materialising the values before the short-circuit.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Range.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Range.swift:25:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Result.swift:21:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Result.swift:22:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Result.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.ReversedCollection.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Set.swift:23:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Set.swift:24:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.Set.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeBufferPointer.swift:18:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeBufferPointer.swift:19:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeBufferPointer.swift:21:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeBufferPointer.swift:22:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeBufferPointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutableBufferPointer.swift:18:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutableBufferPointer.swift:19:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutableBufferPointer.swift:21:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutableBufferPointer.swift:22:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutableBufferPointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutablePointer.swift:18:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutablePointer.swift:19:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutablePointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutableRawPointer.swift:18:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutableRawPointer.swift:19:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeMutableRawPointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafePointer.swift:18:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafePointer.swift:19:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafePointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeRawPointer.swift:18:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeRawPointer.swift:19:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-equation-primitives/Sources/Equation Primitives Standard Library Integration/Equation.Protocol+Swift.UnsafeRawPointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
```
