# Lint pass: swift-hash-primitives — 2026-05-11

**Command**: `swift-linter --all /Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources`
**Tool**: swift-linter @ 17a977715520af9f4f8c1d047da778e9583fb462
**Rules**: all 73 from 13 packs

## Raw findings

```
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Core/Hash.Protocol.swift:20:16: warning: unification_bridge_typealias: [unification_bridge_typealias] [API-NAME-004]: typealias renames a member type to a different local name. Type unification MUST use the canonical type at all call sites; a typealias bridge adds indirection without domain value.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Core/Hash.Protocol.swift:25:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Array.swift:14:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Array.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.ArraySlice.swift:14:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.ArraySlice.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.CollectionOfOne.swift:14:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.CollectionOfOne.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.ContiguousArray.swift:14:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.ContiguousArray.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Dictionary.swift:21:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Dictionary.swift:23:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Dictionary.swift:25:21: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Dictionary.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.EmptyCollection.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.KeyValuePairs.swift:14:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.KeyValuePairs.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Optional.swift:17:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Optional.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.PartialRange.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.PartialRange.swift:16:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.PartialRange.swift:27:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Range.swift:14:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Range.swift:29:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Range.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Range.swift:20:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Result.swift:17:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Result.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.ReversedCollection.swift:14:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.ReversedCollection.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Set.swift:21:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Set.swift:23:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Set.swift:25:21: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.Set.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafeBufferPointer.swift:15:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafeBufferPointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafeMutableBufferPointer.swift:15:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafeMutableBufferPointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafeMutablePointer.swift:15:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafeMutablePointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafeMutableRawPointer.swift:15:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafeMutableRawPointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafePointer.swift:15:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafePointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafeRawPointer.swift:15:17: warning: compound_identifier: [compound_identifier] [API-NAME-002]: methods and properties MUST NOT use compound names. Use nested accessors instead (e.g., `instance.open.write { }` not `instance.openWrite { }`; `dir.walk.files()` not `dir.walkFiles()`). Boolean prefixes (`is`, `has`, `should`, `will`, `did`, `can`, `must`) are exempt; spec-mirroring identifiers are exempt; `package`-scope declarations are exempt per `feedback_compound_package_scope`.
/Users/coen/Developer/swift-primitives/swift-hash-primitives/Sources/Hash Primitives Standard Library Integration/Hash.Protocol+Swift.UnsafeRawPointer.swift:5:15: warning: extension_noncopyable_constraint: [extension_noncopyable_constraint] [MEM-COPY-004]: extensions on `~Copyable`-aware generic types MUST include explicit `where ... ~Copyable` constraints. Without it, the extension is implicitly `where Element: Copyable` and the surface silently shrinks. Add `where Element: ~Copyable` (or the matching constraint name for your type's generic parameter).
```
