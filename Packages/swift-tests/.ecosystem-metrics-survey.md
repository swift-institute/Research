# Ecosystem Metrics Survey

<!--
---
scope: swift-io, swift-pools, swift-witnesses
date: 2026-02-28
last_updated: 2026-04-01
status: DEFERRED
---
-->

Exhaustive survey of all types that aggregate numeric observations across the Swift Institute foundations layer.

---

## 1. swift-io: `IO.Blocking.Threads.Metrics`

**File**: `/Users/coen/Developer/swift-foundations/swift-io/Sources/IO Blocking Threads/IO.Blocking.Threads.Metrics.swift`
**Type path**: `IO.Blocking.Threads.Metrics`
**Classification**: Composite snapshot (gauges + counters + aggregates)
**Visibility**: `public`

### Fields

#### Gauges (point-in-time, can go up or down)

| Field | Type | Description |
|-------|------|-------------|
| `queueDepth` | `Index<Job.Instance>.Count` | Current jobs in queue |
| `acceptanceWaitersDepth` | `Int` | Tasks waiting for queue capacity |
| `executingCount` | `Int` | Jobs currently running on workers |
| `sleepingWorkers` | `Int` | Workers currently sleeping |

#### Counters (monotonically increasing since creation)

| Field | Type | Description |
|-------|------|-------------|
| `enqueuedTotal` | `UInt64` | Jobs successfully enqueued |
| `startedTotal` | `UInt64` | Jobs dequeued by workers |
| `completedTotal` | `UInt64` | Jobs finished |
| `acceptancePromotedTotal` | `UInt64` | Waiters promoted into queue |
| `acceptanceTimeoutTotal` | `UInt64` | Waiters that timed out |
| `failFastTotal` | `UInt64` | Jobs rejected immediately (queue full) |
| `overloadedTotal` | `UInt64` | Jobs rejected (acceptance queue full) |
| `cancelledTotal` | `UInt64` | Jobs cancelled via Task cancellation |

#### Aggregates (latency sample accumulators)

| Field | Type | Description |
|-------|------|-------------|
| `enqueueToStart` | `Aggregate` | Enqueue-to-worker-start latency |
| `execution` | `Aggregate` | Worker-start-to-completion latency |
| `acceptanceWait` | `Aggregate` | Time in acceptance queue (promoted waiters only) |

### Notes

- All values read atomically under the runtime lock (consistent snapshot).
- Aggregate latency recording is wired for `acceptanceWait` but `enqueueToStart` and `execution` are NOT yet recorded (confirmed by test assertions showing `count == 0`).
- Snapshot returned by `threads.metrics()` method.

---

## 2. swift-io: `IO.Blocking.Threads.Aggregate`

**File**: `/Users/coen/Developer/swift-foundations/swift-io/Sources/IO Blocking Threads/IO.Blocking.Threads.Metrics.swift` (lines 206-221)
**Type path**: `IO.Blocking.Threads.Aggregate`
**Classification**: Sample-based latency accumulator
**Visibility**: `public`

### Fields

| Field | Type | Description |
|-------|------|-------------|
| `count` | `UInt64` | Number of samples |
| `sumNs` | `UInt64` | Sum of all durations in nanoseconds |
| `minNs` | `UInt64` | Minimum duration observed |
| `maxNs` | `UInt64` | Maximum duration observed |

### Mutable accumulator

**Type path**: `IO.Blocking.Threads.Aggregate.Mutable` (internal)

```swift
mutating func record(_ durationNs: UInt64)
mutating func record(_ durationNs: Int64)  // guards < 0
func snapshot() -> IO.Blocking.Threads.Aggregate
```

### Notes

- Simple O(1) aggregation: min, max, sum, count.
- No percentiles (P50/P99) -- explicitly deferred to v2, would need HDR histogram or t-digest.
- Average computed by caller: `sumNs / count`.
- Empty state: `count: 0, sumNs: 0, minNs: .max, maxNs: 0`.
- This is the closest thing to a reusable `Measurement` primitive in the ecosystem. Currently coupled to `IO.Blocking.Threads` namespace.

---

## 3. swift-io: `IO.Blocking.Threads.Counters`

**File**: `/Users/coen/Developer/swift-foundations/swift-io/Sources/IO Blocking Threads/IO.Blocking.Threads.Metrics.swift` (lines 35-73)
**Type path**: `IO.Blocking.Threads.Counters`
**Classification**: Lock-free atomic counter bank
**Visibility**: `internal` (class)

### Architecture

- Reference-backed `Cell` wrapping `Atomic<UInt64>` for each counter.
- Nested accessor structs (`Counter`, `BatchCounter`, `Acceptance`, `Affinity`) for fluent property-chain API.
- All use relaxed atomic ordering (telemetry, eventual consistency acceptable).

### Counter accessors

| Accessor path | Type | Description |
|---------------|------|-------------|
| `counters.enqueued` | `Counter` | Total enqueued |
| `counters.started` | `BatchCounter` | Total started (batch-aware) |
| `counters.completed` | `BatchCounter` | Total completed (batch-aware) |
| `counters.acceptance.promoted` | `Counter` | Promoted from acceptance queue |
| `counters.acceptance.timeout` | `Counter` | Timed out in acceptance queue |
| `counters.failFast` | `Counter` | Rejected with failFast |
| `counters.overloaded` | `Counter` | Rejected with overloaded |
| `counters.cancelled` | `Counter` | Cancelled via Task |
| `counters.affinity.failures` | `Counter` | Affinity routing failures |

### `Counter` vs `BatchCounter`

- `Counter`: `increment()`, `add(_ count: Int)`, `value: UInt64`
- `BatchCounter`: `increment()`, `add(_ count: some Cardinal.Protocol)`, `value: UInt64`
- The distinction is for typed batch increments via `Cardinal.Protocol`.

---

## 4. swift-io: `IO.Blocking.Threads.Runtime.State.Gauge`

**File**: `/Users/coen/Developer/swift-foundations/swift-io/Sources/IO Blocking Threads/IO.Blocking.Threads.Runtime.State.swift` (lines 356-419)
**Type path**: `IO.Blocking.Threads.Runtime.State.Gauge`
**Classification**: Lock-free advisory gauges
**Visibility**: `internal`

### Fields (via `Gauge.Storage`)

| Accessor | Backing | Description |
|----------|---------|-------------|
| `gauge.queueDepth` | `Atomic<Int>` | Queue depth (may be stale) |
| `gauge.acceptanceDepth` | `Atomic<Int>` | Acceptance waiters (may be stale) |
| `gauge.sleepers` | `Atomic<Int>` | Sleeping workers (may be stale) |

### Notes

- Updated under lock before unlock (`gauge.update()`).
- Read lock-free for best-effort routing (sharded lane selector).
- Explicitly advisory -- "not suitable for correctness checks."

---

## 5. swift-io: `IO.Blocking.Threads.DebugSnapshot`

**File**: `/Users/coen/Developer/swift-foundations/swift-io/Sources/IO Blocking Threads/IO.Blocking.Threads.DebugSnapshot.swift`
**Type path**: `IO.Blocking.Threads.DebugSnapshot`
**Classification**: Test-only state snapshot
**Visibility**: `public`

### Fields

| Field | Type | Description |
|-------|------|-------------|
| `sleepers` | `Int` | Sleeping workers |
| `queueIsEmpty` | `Bool` | Whether queue is empty |
| `queueCount` | `Index<Job.Instance>.Count` | Queue depth |
| `inFlightCount` | `Int` | In-flight jobs |
| `isShutdown` | `Bool` | Shutdown state |

---

## 6. swift-io: `IO.Blocking.Lane.Abandoning.Metrics`

**File**: `/Users/coen/Developer/swift-foundations/swift-io/Sources/IO Blocking/IO.Blocking.Lane.Abandoning.Metrics.swift`
**Type path**: `IO.Blocking.Lane.Abandoning.Metrics`
**Classification**: Composite snapshot (gauges + counters)
**Visibility**: `public`

### Decomposed into three nested structs

#### `Metrics.Workers`
**File**: `IO.Blocking.Lane.Abandoning.Metrics.Workers.swift`

| Field | Type | Classification | Description |
|-------|------|----------------|-------------|
| `abandoned` | `Kernel.Thread.Count` | Counter | Workers abandoned due to timeout |
| `active` | `Kernel.Thread.Count` | Gauge | Currently active workers |
| `spawned` | `Kernel.Thread.Count` | Counter | Total workers spawned |

#### `Metrics.Queue`
**File**: `IO.Blocking.Lane.Abandoning.Metrics.Queue.swift`

| Field | Type | Classification | Description |
|-------|------|----------------|-------------|
| `depth` | `Int` | Gauge | Current queue depth |

#### `Metrics.Total`
**File**: `IO.Blocking.Lane.Abandoning.Metrics.Total.swift`

| Field | Type | Classification | Description |
|-------|------|----------------|-------------|
| `completed` | `UInt64` | Counter | Total operations completed |
| `abandoned` | `UInt64` | Counter | Total operations abandoned |

### Notes

- Snapshot taken under Mutex lock in `Runtime.metrics()`.
- No latency aggregates (no `Aggregate` equivalent).
- Simpler than `IO.Blocking.Threads.Metrics` -- no acceptance queue machinery.

---

## 7. swift-io: `IO.Handle.Registry.Metrics`

**File**: `/Users/coen/Developer/swift-foundations/swift-io/Sources/IO/IO.Handle.Registry.swift` (lines 1007-1027)
**Type path**: `IO.Handle.Registry<Resource>.Metrics`
**Classification**: Minimal gauge snapshot
**Visibility**: `internal`

### Fields

| Field | Type | Classification | Description |
|-------|------|----------------|-------------|
| `registeredCount` | `Int` | Gauge | Currently registered handles |
| `lifecycleState` | `IO.Lifecycle` | Gauge | Pool lifecycle state |

---

## 8. swift-io: `IO.Executor.Counter`

**File**: `/Users/coen/Developer/swift-foundations/swift-io/Sources/IO/IO.Executor.Counter.swift`
**Type path**: `IO.Executor.Counter`
**Classification**: Atomic sequence counter (not a metric)
**Visibility**: `internal`

### Notes

- Simple `Atomic<UInt64>` with `next() -> UInt64` (relaxed wrapping add).
- Used for generating unique scope IDs (`IO.Executor.scopeCounter`).
- NOT a metric counter -- it is an ID generator. Included for completeness.

---

## 9. swift-pools: `Pool.Blocking.Metrics`

**File**: `/Users/coen/Developer/swift-foundations/swift-pools/Sources/Pool/Pool.Blocking.Metrics.swift`
**Type path**: `Pool.Blocking.Metrics`
**Classification**: Composite snapshot (gauges + counters)
**Visibility**: `public`

### Fields

| Field | Type | Classification | Description |
|-------|------|----------------|-------------|
| `acquisitions` | `UInt64` | Counter | Total successful acquisitions |
| `releases` | `UInt64` | Counter | Total releases |
| `rejected` | `UInt64` | Counter | Total rejected (shutdown) |
| `timeouts` | `UInt64` | Counter | Total timed-out acquisitions |
| `cancellations` | `UInt64` | Counter | Total cancelled acquisitions |
| `outstanding` | `Int` | Gauge | Currently outstanding permits |
| `available` | `Int` | Gauge | Currently available permits |
| `waiters` | `Int` | Gauge | Currently waiting callers |
| `peak` | `Int` | High-water mark | Maximum simultaneous outstanding |

### Notes

- Metrics mutated inline during acquire/release under `Kernel.Thread.DualSync` lock.
- `waiters` is added at read time from `_state.waiters` (not stored in metrics struct).
- No latency aggregates (no timing data at all).
- `peak` is the only "derived" metric -- a running maximum.

---

## 10. swift-witnesses

**Location**: `/Users/coen/Developer/swift-foundations/swift-witnesses/Sources/`

### Assessment

No measurement-related types found. The Witnesses package provides dependency injection and testing infrastructure (`Witness.Key`, `Witness.Recording`, `Witness.Sequence`, etc.). The `Counter` and `Gauge` grep hits were false positives -- they match against unrelated uses of those words in macro implementation code and are not metric types.

---

## 11. Packages NOT Found

The following packages do not exist in the foundations layer:

| Package | Status |
|---------|--------|
| swift-http | Does not exist |
| swift-log | Does not exist |
| swift-tracing | Does not exist |
| swift-health | Does not exist |
| swift-scheduler | Does not exist |

---

## 12. Benchmark Patterns (swift-io Tests)

**Location**: `/Users/coen/Developer/swift-foundations/swift-io/Tests/IO Benchmarks/`

### Files surveyed

| File | What it measures |
|------|-----------------|
| `ThroughputBenchmarks.swift` | Ops/sec with simulated blocking work |
| `BackpressureBenchmarks.swift` | Rejection latency, wait behavior, bounded memory |
| `ContentionBenchmarks.swift` | Fairness under thread contention (10:1, 100:1, 1000:1) |
| `LifecycleBenchmarks.swift` | Creation and shutdown costs |
| `SchedulingLatencyBenchmarks.swift` | Executor thread scheduling latency |
| `OverheadBenchmarks.swift` | Framework dispatch overhead |
| `CancellationBenchmarks.swift` | Pre/post-acceptance cancellation costs |
| `MemoryBenchmarks.swift` | Per-operation allocations, boxing, sustained pressure |

### Benchmark infrastructure types

| Type | Location | Description |
|------|----------|-------------|
| `IOBenchmarkFixture` | `Tests/Support/IOBenchmarkFixture.swift` | Shared pre-configured lane |
| `ThreadPoolTesting` | `Tests/Support/IO Test Support.swift` | `waitUntilIdle()` utility |
| `BlockerLatch` | `BackpressureBenchmarks.swift` | Atomic barrier for test synchronization |
| `SaturatedLaneTrait` | `BackpressureBenchmarks.swift` | TestScoping trait for pre-saturated lane |

### Key observation

Benchmarks use Swift Testing `@Test` annotations but do NOT use any formal measurement framework. They rely on:
- Wall-clock timing (implicit from test runner)
- `ContinuousClock` for work simulation (`simulateWork()`)
- Counting completed/rejected operations manually
- `IO.Blocking.Threads.Metrics` for verifying counters after runs

There is no shared `Benchmark.Result` or `Measurement.Sample` type -- each benchmark is a standalone scenario with ad-hoc assertions.

---

## Analysis: Shared Primitive Opportunity

### Pattern summary

| Pattern | Where used | Current implementation |
|---------|------------|----------------------|
| **Monotonic counter** (UInt64, relaxed atomic) | IO.Blocking.Threads.Counters, Pool.Blocking.Metrics, Abandoning.Metrics.Total | Ad-hoc per-site |
| **Gauge** (Int, point-in-time) | IO.Blocking.Threads.Metrics, Pool.Blocking.Metrics, Abandoning.Metrics.Queue, Registry.Metrics | Ad-hoc per-site |
| **High-water mark** (running max) | Pool.Blocking.Metrics.peak | Single site |
| **Latency aggregate** (count/sum/min/max) | IO.Blocking.Threads.Aggregate | Single site, coupled to IO |
| **Advisory atomic gauge** (stale-OK) | IO.Blocking.Threads.Runtime.State.Gauge | Single site |
| **Snapshot** (consistent read under lock) | Both Metrics types | Ad-hoc per-site |

### Could benefit from a shared primitive

1. **`Aggregate` (or `Sample.Aggregate`)** -- The `IO.Blocking.Threads.Aggregate` + `Aggregate.Mutable` pattern is completely domain-independent. It tracks `{count, sumNs, minNs, maxNs}` with O(1) `record()` and `snapshot()`. This could be extracted to a primitives-layer type and reused by:
   - `IO.Blocking.Lane.Abandoning.Metrics` (currently has no latency tracking)
   - `Pool.Blocking.Metrics` (currently has no timing data)
   - Future health checks, scheduling metrics, etc.

2. **Atomic counter bank pattern** -- The `Cell`-backed `Counter`/`BatchCounter` pattern from `IO.Blocking.Threads.Counters` is reusable but currently tied to that namespace. A primitives-layer `Atomic.Counter` with the same `increment()`/`add()`/`value` API could standardize this.

3. **`Metrics` snapshot protocol** -- Both `IO.Blocking.Threads.Metrics` and `Pool.Blocking.Metrics` follow the same pattern: read state under lock, return frozen struct. There is no shared protocol or trait for "provides metrics." A `Metrizable` protocol or witness could unify observability.

### Would NOT benefit from extraction

- **Advisory gauges** -- The `Gauge.Storage` pattern is tightly coupled to the lock/update lifecycle of its specific runtime state. Extracting it would not add value.
- **`DebugSnapshot`** -- Test-only, intentionally coupled to specific internal state.
- **`IO.Executor.Counter`** -- ID generator, not a metric.
- **`Pool.Blocking.Metrics.peak`** -- Too simple to warrant extraction (just a running max).

### Priority recommendation

The single highest-impact extraction is `Aggregate`/`Aggregate.Mutable`. It is:
- Fully domain-independent (only depends on `UInt64`)
- Already production-tested in swift-io
- The only latency tracking primitive in the ecosystem
- Missing from swift-pools (which tracks no timing data at all)
- Missing from the abandoning lane (which has worker/queue metrics but no latency)
- A natural home for future percentile support (HDR histogram, t-digest)
