# Staged text 3 of 3 — dedicated assessment (Draft D, final)

    record-kind: staged-github-comment
    destination: comment on the issue opened from staged text 2 of 3
      (goal-agent-first-authoring-standard.md), in swift-institute/.github
    status: staged, unposted as of 2026-07-30 — posting and acceptance are principal-gated
    prepared-from: swift-institute/Research@cccc44417100dfcdadee652b17cb44060c573fd5,
      agent-first-presence/agent-first-presence-drafts.md (Draft D) and
      agent-first-presence/agent-first-presence-final-report.md
    grammar-version: staged-text/1

Filing instruction. Post on the Goal issue. This is the strongest-class dedicated assessment
required before admission
(https://github.com/swift-institute/.github/issues/82#issuecomment-5122540856). Every count
freezes here or in a linked receipt, never in the Goal body. Acceptance is a separate dated
principal comment; acceptance activates the Goal but does not admit it to the Project — the
admission decision creating the row's membership and Priority is distinct.

Slots filled at finalization: the frozen-measurement receipt coordinate in §1 (the research
corpus is content-addressed and public, so it resolves without a further artifact) and the
supersession enumeration in §3 (exact sentences, by durable coordinate). One recorded fact
was added at §7.1 (hierarchy of the make-safe pre-pass). The §2 readership ruling and the §6
enforcement-host and mutation-channel rulings carried no open slot — the principal's rulings
were already recorded in place in the corpus draft, and are reproduced here unchanged.

One correction found at finalization and recorded in §3: the github skill's superseded
issue-type enumeration ("there is no fourth") no longer exists, so it needs no supersession
under this Goal.

---

```md
Dedicated assessment — agent-first authoring standard (2026-07-30)

1. Disease and evidence. Fleet recurrence of stale derived content
   (whole-population surveys over three layers, 451 local package roots: every
   class of hand-restated fact has rotted at 23–61% with zero renames
   involved), plus the unowned reference-durability defect (bare display
   names, unqualified cross-repository references, zero durable ProjectV2
   coordinates in local record trees). The honest split: about 350 statements
   are objectively false today against manifest or remote under current
   doctrine; the larger latent-fragile mass is standard-relative and is cited
   as conversion scope only, never as objective defect mass.

   Frozen measurement receipt — the whole-population audits, the literature
   and prior-art surveys, the enforcement inventory, the migration costing,
   the adversarial critique, and the live-GitHub verification, immutable at
   commit `cccc44417100dfcdadee652b17cb44060c573fd5` of
   https://github.com/swift-institute/Research, directory
   `agent-first-presence/`. Git blob digests (SHA-1), each resolvable at that
   commit:

   | File | Blob |
   |---|---|
   | `estate-primitives.md` | `cfa34caac2a1b2caab4b8ba2266686d5ce26cee5` |
   | `estate-standards-records.md` | `228c6f976814afcc09f341b52fb46c9857a541e5` |
   | `estate-foundations.md` | `74f47265cf0506c6fb601f2051f087d0f0f4de8f` |
   | `enforcement.md` | `cd6ec08fe5fca0418f520b964bfdd7d9f03eae5c` |
   | `migration.md` | `155baaba78b7897e0237caad2fc790e051b33421` |
   | `critique.md` | `df6d47d8fed785932f9586ee62a89cda00adb2ee` |
   | `gh-live.md` | `b8e5dd4c6d2e98d0dbf45381930b5df346d81b91` |
   | `literature.md` | `4bef7d79c3ffa267e19d163e1b78ae61468d750a` |
   | `prior-art.md` | `5a7f38d2a1dbe8e8a30f33bef6bab3e644dc8234` |
   | `agent-first-presence-final-report.md` | `b05e5d6d8ed004a2061377b88efe5b928c083e90` |

   The fleet-wide remote CI measurement is not restated here; it is owned by
   https://github.com/swift-institute/.github/issues/94#issuecomment-5129689259.

   Honest framing: this is the same disease as
   https://github.com/swift-institute/.github/issues/79 at fleet scale, plus a
   new standards layer. #79's sealed remediation set never included these
   surfaces, and its children never touched the package estate.

2. Readership ruling (principal, 2026-07-30). Agents are the primary readers
   of READMEs and records; no such surface may depend on human memory or
   human-only rendering. DocC is the designated human-oriented surface, and
   Swift Package Index adoption is a near-term objective: the SPI-live
   placeholder catalogues are near-term priority, the SPI publish gate moves
   ahead of the layer sweeps, and DocC completion is preferred over deletion
   for SPI-visible packages. Organization profiles and social previews are
   in-scope investments. No deletion in this programme is justified by reader
   denial; each is justified by measured rot or measured dead weight.

3. Page-content contract: deletion-first. Derived facts (status badges,
   toolchain and platform floor prose, numeric count claims, bare
   product/target/platform tables, dead community blocks) are deleted, not
   regenerated; the manifest and the evaluated-manifest command are the
   projection. Kept: identity, when-to-use judgment, authored product naming
   (satisfies https://github.com/swift-institute/.github/issues/80 as
   designed), one quick-start line, canonical links, one normalized install
   snippet under the ratified pin rule with a currency predicate.

   Supersessions this requires, by exact sentence. Locator:
   https://github.com/swift-institute/Skills, `documentation/readmes.md`, blob
   `f43ab53706ff9f9c28ed3835bfafebfe6bb77914` at commit
   `3a4c0d4fc02308dcd362f375fd8fd7e21d87ee7f` (the revision assessed).

   | # | Exact sentence or clause | Disposition |
   |---|---|---|
   | S1 | "**Minimum, always** — title, development-status badge, one-liner, `## Installation`, `## License`." | Superseded in part: the `development-status badge` item is struck; the remaining items stand. |
   | S2 | "The development-status badge is the first badge line, directly after the H1, and uses the standard status vocabulary (`status-active--development-blue`, not an improvised `status-beta-yellow`)." | Superseded in full: one unfalsifiable claim copied fleet-wide. |
   | S3 | "**Standard, once there is a public API** — plus Key Features, Quick Start, Architecture, Platform Support." | Superseded in part: `Platform Support` is struck as a required section; the remaining items stand. |
   | S4 | "Platform Support cells use only `Full support`, `Supported`, `Planned`, `Possible`, `Not supported`." | Superseded in full together with S3: the platform matrix is derived from the manifest. |
   | S5 | "The `## Installation` block carries both a dependency clause and a target clause, and the pin form matches reality: a branch pin before the package has any tag, a `from:` tag pin only once that tag exists." | **Retained**, explicitly. The pin rule is ratified and correct; this programme adds mechanical currency enforcement (the designed-but-unimplemented README version-currency predicate) rather than superseding the rule. Recorded here so no sweep reads the badge and platform supersessions as licence to delete install blocks. |
   | S6 | The section "Where the check and the text disagree", in full. | **Retained as a standing obligation**: each predicate that lands under this Goal updates that section in the same transaction, per the honest-voice rule that a page must disclose what is and is not checked. |
   | S7 | The org-profile paragraph forbidding package enumeration ("an org profile must not enumerate packages… Route into the native tab with one filter link per domain"). | **Retained and cited**: already-ratified doctrine that the remaining organization-profile conversions implement. |

   Each supersession lands as a dated in-place amendment at the semantic owner,
   never as a silent edit, and in the same transaction as the README rule-family
   changes that enforce the struck sentences. The exact rule identifiers are
   resolved against the validator manifest's rule-to-validator binding at
   supersession time rather than asserted here.

   Two additive changes, not supersessions: `documentation/docc.md` gains the
   no-manifest-restatement sentence for `.docc` articles, and the github skill
   gains the durable-coordinate prescription for future comments and records
   (the display-name defect is wider than any one sentence, and comments are
   never edited).

   Correction to the pre-ruling draft: that draft listed "the github skill's
   superseded issue-type enumeration" as a supersession this Goal must record.
   It is already landed — the sentence asserting three enabled types with "no
   fourth" was removed from `github/SKILL.md` in `swift-institute/Skills`
   commit `dcb4793371544c246ec700c6f683e3fc3fc29149`, and the assessed
   revision above (blob `cc1eabf59e69150093d3ace06f87f83750b12d39`) enumerates the `Goal`
   type as live. No supersession is required, and the Goal body no longer
   claims one.

4. Identifier standard. Trusted-coordinate ranking (content digest with
   locator > commit-SHA permalink > canonical URL and container-scoped number
   > node identifiers as cache only > display names as dated glosses); the
   pair rule (durable coordinate paired with a semantic gloss) with
   define-once-per-record; canonical spellings retained as lexical anchors,
   because order within the pair is style and the pairing is the substance
   the predicate checks; the never-reclaim rule for repository and
   organization names; a resolution-audit predicate; organization names
   treated as the highest-blast-radius renameable coordinate, the estate
   being multi-organization. No Institute identifier namespace is minted.

5. Record grammar. Minimal required core (kind, owner coordinate, status,
   grammar version); self-describing version; declared backward
   compatibility, so that every conforming record stays valid under the next
   version or the change ships with a filed migration sweep; retired kinds
   and fields reserved, never recycled; prospective-only with an explicit
   grandfather clause, so ratification manufactures no retroactive violation
   in any already-accepted record; the full section list ships as the Goal
   issue form's rendering, not as the required core.

6. Enforcement placement. Predicate catalogue with owners (records
   reconciler for live GitHub objects, the ruled page-predicate host for
   repository files, Workspace for manifests and receipts, issue forms at
   authoring time, skills for judgment; swift-linter excluded by input
   domain, being AST-only over `**/*.swift` by construction). Fail-closed
   fixtures per the ratified positive/negative/fail-closed model; every
   predicate reports `unmeasured`, never clean, when it could not measure;
   report-only permanently where false positives are inherent, with measured
   precision; deny ratchets key on in-repository machine state, never on
   issue or label state.

   Enforcement-host ruling (principal, 2026-07-30): the Swift policy-tool
   direction, not the Python validator fleet. The evaluated-manifest
   predicates require the Swift manifest model regardless, and new predicates
   land report-only first. The existing Python README and DocC rule families
   keep their surfaces; this ruling governs new predicates.

   Mutation-channel ruling (principal, 2026-07-30): bot push for mechanical
   sweeps and for the make-safe pre-pass, which is fleet-wide precedented;
   per-repository review with semantic-review receipts only for the judgment
   wave.

7. Programme shape. Make-safe pre-pass (already ordinary Bug work); doctrine
   before pages; instruments report-only before canary; sealed cohorts with
   content-addressed receipts and per-file pre-conversion blob digests as the
   rollback map; two-commit discipline separating deletions from any marker
   injection; a machine-readable conversion marker in repository metadata as
   the half-converted-state guard; judgment wave last under the
   five-disposition rule; instrumented canary with a defined agent-evaluation
   protocol and the abort path stated in the Goal body.

7.1 Recorded fact — hierarchy of the make-safe pre-pass. The make-safe Bug
   issues are filed before this Goal exists, so they cannot carry the
   at-filing native sub-issue link that
   https://github.com/swift-institute/.github/issues/68#issuecomment-5132050182
   requires of an exact-owner Issue filed in execution of a Goal. The omission
   is planned, not accidental. This assessment commits to exactly one
   retro-linking pass at activation, following the backfill pattern of
   https://github.com/swift-institute/.github/issues/124: a criterion comment
   posted on this Goal first, stating the child-versus-mention test applied,
   then the sub-issue links for the issues that satisfy it, closed children
   included, with a GraphQL read-back whose sub-issue count matches the
   criterion comment's list. The pass is one serial operation, and no second
   sweep is chartered; every child filed after activation links at filing like
   any other. Hierarchy remains evidence and not activation: linking a
   make-safe issue asserts that the corrected surface belongs to this Goal's
   outcome, not that the correction waited on this Goal — it did not.

8. Alternatives considered. Evolve #79 — rejected: it couples the portfolio's
   most mature convergence to its largest new workload and destroys the
   finite-episode property that makes its receipts meaningful; the
   consume-not-absorb precedent recorded on
   https://github.com/swift-institute/.github/issues/94 ("a prerequisite …
   not a child objective") is the fitting shape. Two-stage Goals — rejected:
   the in-Goal canary gate with a named abort path buys the same honesty
   about unmeasured benefit without a second principal-acceptance cycle,
   which is the measured long pole. Doctrine-as-Tasks without a Goal —
   rejected for the conversion, which needs portfolio visibility, sealed
   cohorts, receipts and rollback; adopted in spirit, in that the doctrine
   children execute immediately on acceptance. Defer — rejected: the estate
   is stating falsehoods today, and ratified authoring doctrine currently
   mandates what the evidence condemns.

9. Cost and gates. The envelope and its sensitivity are assessed in the
   frozen measurement receipt of §1; the mutation channel dominates every
   other cost lever, and §6 rules it. The #79 gate constrains cohort sealing
   only and resolves on closure of #79 or on a scope-pinning ruling,
   whichever is earlier; the principal's activation-cohort scope pin
   (2026-07-30) is staged at
   `agent-first-presence/staged/79-audit-scope-pin-ruling-comment.md` in
   https://github.com/swift-institute/Research and satisfies this gate once
   posted on #79. #80 completes on predicate-exists, its findings being
   exact-owner work.

   Research-tree exemption granted (principal ruling, 2026-07-30): the
   `Research/` tree is archival, its bare references are left as they are, and
   its generated index remains its navigation surface.

   Benefit is stated honestly: no controlled study links page format to agent
   task success. The cost of the status quo is measured; the benefit claim
   gets its first measurement at the canary, and the abort path is what makes
   that claim honest.
```
