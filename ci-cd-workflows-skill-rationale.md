# CI/CD Workflows Skill Rationale Archive

<!--
version: 1.0.0
last_updated: 2026-07-05
status: REFERENCE
-->

> Non-normative companion to `Skills/ci-cd-workflows/SKILL.md` (per Research/ecosystem-meta-setup-target-state.md §D1).
> This document holds evicted incident narratives, provenance notes, bookkeeping audit trails, and
> relationship-to-adjacent-rule essays. The skill file remains the CANONICAL source for all `[CI-*]`
> requirement statements; nothing in this archive is normative. Organized by rule ID.

---

## §[CI-044] Tool-Binary Cache — Known Non-Conformances (evicted 2026-07-05)

Bookkeeping audit trail evicted from the skill; both entries are resolved. The present-tense rule body in-skill is the canonical corpus; this is the translated live-state record.

### Known Non-Conformances

The following live workflow files did not satisfy the caching rules. Each is tracked for follow-up; entries are **not** deleted on resolution — the fix-commit SHA is appended and the row marked "Resolved" so the historical record survives.

| File | Violation | Rule(s) | Status |
|---|---|---|---|
| `swift-primitives/.github/.github/workflows/swift-ci.yml` (embedded job) | Carried `restore-keys: linux-embedded-${{ inputs.cache-key-prefix }}-` partial-prefix fallback alongside an otherwise-correct exact-match `key:`. | [CI-042] | **Resolved `44b5acb`** (2026-05-04) — single-line removal of `restore-keys:`; `key:` retained as exact-match per the [CI-040] carve-out. |
| `swift-institute/.github/.github/workflows/swift-docs.yml` (docs job) | Cached `.build/` with `key: macos-${{ inputs.cache-key-prefix }}-${{ hashFiles(...) }}` AND `restore-keys: macos-${{ inputs.cache-key-prefix }}-` partial-prefix. | [CI-040], [CI-042] | **Resolved `2d1f6b8`** (2026-05-04) — entire `actions/cache@v5` step removed (no carve-out applied: DocC traverses the same dependency graph as the universal matrix). |

**Resolution discipline**: when a fix lands, append the fix-commit SHA to the row's Status column (e.g., "Resolved `abc1234`") in the same commit-wave that lands the fix. The aspirational citation per [SKILL-LIFE-027] is satisfied by the present-tense rule body; this sub-section is the bookkeeping that translates "the rule corpus is correct" into "the live state matches the corpus."

---

## §[CI-110] Mixed-Trigger Null-Coerce — Provenance Notes (evicted 2026-07-05)

**Provenance note — the tab-then-anchor grep-trap in `validate-base.yml` (2026-07-03, non-normative)**: the same regex-tooling-trap class bit the `validate-base.yml` "Aggregate findings" step. It counted violations with `grep -cE "<TAB>${RULE_ID_REGEX}" /tmp/findings.tsv`, while every one of the 18 thin callers passes a `^`-anchored `rule-id-regex` (`^CI-032`, `^PATTERN-`, …). The effective pattern `<TAB>^CI-NNN` placed a line-start anchor mid-pattern and matched ZERO lines, so `violations` was structurally pinned to 0, the gate never fired, and (with the validator's own exit code separately swallowed by `|| true` at the Run-validator step) all 18 validate-base consumers were rendered inert and reported "Total violations: 0" false-green. Fixed by reusing the summary table renderer's field-match predicate (`awk -F'\t' '$2 ~ re'`) for the count so the count and the table share one predicate and can never disagree; a clone-failed sentinel gate was added in the same change. Fix: swift-institute/.github `12c9d5a`. This note records the incident; it makes no change to any Statement.

**Follow-up (2026-07-05, Phase-3 review, non-normative)**: the `|| true` exit-code swallow flagged above was itself a residual false-green — a validator that cloned cleanly then crashed (an uncaught exception, or `validate_lib.require_yaml()`'s import-time `sys.exit(2)` on missing PyYAML) wrote zero TSV rows, so the run reported "Total violations: 0" green with the validator never having produced usable output. Closed by a `validator-crashed` sentinel that mirrors the clone-failed mechanism exactly: the Run-validator step now captures the validator's exit code without tripping `set -e` (`python3 "$VALIDATOR" … >> /tmp/findings.tsv || rc=$?`) and appends a `<repo><TAB>validator-crashed<TAB>-` row on nonzero rc — genuine, because the validators are signal-via-TSV and return 0 even with findings, so nonzero means crash/abort, not violations. The aggregation counts those rows (kept out of the `RULE_ID_REGEX` violation count exactly as clone-failed is, since `validator-crashed` matches no `^`-anchored rule regex), surfaces a CAUTION block + an `::error::` annotation, and includes them in the exit-1 gate. Reproduced with the step's verbatim shell logic (pre-fix crash greens; post-fix exits 1). Fix: swift-institute/.github `0abdfa1`. This note records the incident; it makes no change to any Statement.

**Provenance note — validator fail-fixture backfill (2026-07-03, non-normative)**: distinct from the grep-trap above, a companion non-inertness gap surfaced in the same review — four validators (`docc-structure`, `readme`, `github-metadata`, `package-structure`) carried only PASS fixtures, with no FAIL fixture proving the validator actually fires on a known-bad input. A pass-only fixture set cannot distinguish a working validator from an inert one — exactly the false-green class the grep-trap produced. **Two of the four were fixtured this arc** (fail+pass, registered in `run.sh`): `validate-docc-structure` (fixture `doc-020`) and `validate-readme` (fixture `readme-017`). **The other two remain a residual harness-coverage gap within the per-repo-scan (`validate-base`/TSV) validator family**: `validate-github-metadata` and `validate-package-structure` do not fit `run.sh`'s uniform 2-arg dir-scan contract — github-metadata needs a 3-arg `<repo> <metadata-yaml> <schema-json>` invocation plus `jsonschema`/PyYAML, and package-structure consumes `swift package describe --type json`. Their non-inertness was confirmed out-of-band 2026-07-03: the seat positive-controlled `package-structure` directly (crafted fail JSON → `MOD-012-domain-undetectable`; pass JSON → 0 findings), and `github-metadata` — not locally runnable here (missing deps) — has a loud-error failure mode (not silent-pass) and runs in CI. Both are tracked as a successor item: extend `run.sh` to a per-validator invocation contract (plus a `swift package describe`-JSON fixture mode) to close automated regression coverage. **Out-of-family (2026-07-05, Phase-3 review)**: `validate-schema-workflow-keys.py` (post-arc `fa505a6`, [GH-REPO-063] domain; zero-arg, self-locating, non-TSV, embedded as a single step in `validate-github-metadata.yml` alongside — but distinct from — the `validate-github-metadata.py` TSV validator above) is NOT part of this residual gap: it isn't a per-repo-scan/TSV-family validator at all, so `run.sh`'s 2-arg contract doesn't apply to it. Its fixture tracking belongs under the **github-repository** skill charter, not here. This note records the provenance; it makes no change to any Statement.

---

## §[CI-111] Mass Mechanical Rewrite Scripts — Sub-Shapes, Provenance, Relationship (evicted 2026-07-05)

**Two failure sub-shapes the skip-check prevents**:

| Sub-shape | Example |
|-----------|---------|
| **Re-splitting** — the extraction regex treats both fresh targets AND already-transformed forms as input | `` `flatMap transforms and flattens` `` → `` `flat map transforms and flattens` `` (the API reference `flatMap` is split apart) |
| **Case-destruction** — stripping + re-splitting a backticked span lowercases sentence-start caps and splits symbols | `` `Int(bitPattern: x.rawValue) is flagged` `` → `` `int(bit pattern: x.rawvalue) is flagged` `` |

Both arise from an identifier-extraction regex of the form `` (`[^`]+`|[a-z][a-zA-Z0-9_]*) `` that matches pre-existing backticked names alongside fresh camelCase, then unconditionally strips backticks and re-splits.

**Provenance (full form)**: 2026-05-15 camelCase→backticked `@Test`-name rename across the data-structures cohort. The extraction regex matched both fresh camelCase and pre-existing backticked names; `transform_name` stripped backticks and re-split, producing 125 cosmetic regressions across sequence-primitives (37 backticked names destroyed), cyclic-primitives (41 destroyed, sentence-start caps lowercased), and primitives-linter-rules (47 destroyed, symbols split inside backticked names). Every case still passed the lint rule (the name still contained a space) — the regression was invisible to canary verification and surfaced only on diff inspection during the cascade commit; all three packages were reverted. Concurrent parallel-iteration churn (`feedback_parallel_workspace_no_build`) meant canary verification was already shaky, shifting additional regression-detection responsibility onto the author. Memory `feedback_rename_script_preserve_backticked`.

**Relationship to [CI-051]/[CI-056]**: [CI-051] covers surgical-commit + dirty-skip discipline; [CI-056] gates source-modifying rollouts on per-package build-verify. [CI-111] adds the transform-CORRECTNESS gate for mass mechanical rewrites — a build-green, lint-green rewrite can still be semantically wrong, so a sample diff-inspection (and pre-existing-form skip) is mandatory before commit.

---

## §[CI-112] Clean-Room Resolve — Why Substitutes Fail, Safe-Run Recipe, CI-Job Spec (authored 2026-07-05)

**Why substitutes fail (2026-06-02, ecosystem path→URL arc)**: the clean-room was argued redundant twice ("grep=0 + `git ls-remote` reachability + a mirror-backed resolve prove the same thing"). It did not — the mirror-bypassed resolve caught TWO real off-machine bugs every substitute missed:

1. **Empty / unpushed dependency repos.** `git ls-remote <url> HEAD` returns exit 0 on an EMPTY GitHub repo, so a reachability sweep counts it "reachable." `swift-binary-serializer-primitives` had 8 local commits but `isEmpty:true` on GitHub → the mirror resolved it from the local clone; off-machine it has no `main`. Fix: push it. Sweep for `refs/heads/main` presence, not just `ls-remote HEAD` exit code.
2. **Off-machine package identity.** When a dep's local-dir basename ≠ its git repo name, the mirror aliases BOTH names, but off-machine only the URL repo-name is valid → every `.product(package: "<basename>")` breaks. This is the [PKG-DEP-008] failure class.

A mirror-backed `swift package resolve` only proves the GRAPH is internally consistent against LOCAL clones; it cannot prove off-machine fetchability or off-machine identity.

**Safe-run recipe on PRIVATE repos** (no public flip, no secrets, no token injection): keep the REAL session — its ambient auth already works (that is how pushes succeed). Do NOT use a temp HOME (it drops the credential and is what tempts token injection). Instead: confirm no `swift-frontend`/build is running; briefly `mv ~/.swiftpm/configuration/mirrors.json` aside; `git clone` a consumer-root fresh + `swift package resolve` (fetches from GitHub via ambient auth); restore the mirror via a `trap`-guarded self-restoring script so it is absent for exactly the one resolve. NEVER inject a token to force it (memory `feedback_no_credential_injection_for_verification`).

**CI-runner form (clean-runner resolve job)**: a scheduled/dispatched job on the PUBLIC `swift-institute/.github` hub (free Actions minutes per [CI-096]; not private-skipped per the [CI-095] dispatch-surface rider). Matrix over public consumer-root repos. Each cell: `actions/checkout` the consumer ONLY — a fresh runner has no `~/.swiftpm/configuration/mirrors.json`, so `swift package resolve` is mirror-bypassed BY CONSTRUCTION; resolve against the real GitHub remotes (bot App token for private-dep access); assert exit 0. Additionally sweep each resolved dep's remote for `refs/heads/main` presence (not merely `git ls-remote <url> HEAD` exit code). It fails on unpushed/empty dep repos and off-machine `.product(package:)` identity mismatches ([PKG-DEP-008]) exactly as the local recipe does. Gating posture: advisory during soak; promote to gating before any publication / public-flip claim. Precedent: [CI-094]/[CI-095] private-repo / off-machine verification surface.

Memory `feedback_clean_room_resolve_not_redundant`.

---

## §Changelog-Provenance

Evicted from SKILL.md frontmatter 2026-07-05 (corpus review) — entries verbatim:

```
# 2026-07-05 (Phase-3 review corrections, source: Audits/REPORT-ci-refactor-2026-07-03.md §7): (1) pin-site recount 34→35 sites for `actions/create-github-app-token` (26 in `.github/workflows/*.yml` + 9 in `.github/actions/configure-private-repos/action.yml`; the 26th is the arc's own `startup-failure-probe-weekly.yml`, `881696f`) at both the [CI-107] top-of-file changelog entry and the [CI-107] rule-body Provenance — recounted against swift-institute/.github @ 560a8da. (2) [CI-110] validator fail-fixture backfill note rescoped: the "residual harness-coverage gap" (`validate-github-metadata` + `validate-package-structure`) is scoped to the per-repo-scan (`validate-base`/TSV) validator family; `validate-schema-workflow-keys.py` (post-arc `fa505a6`, [GH-REPO-063] domain; zero-arg, self-locating, non-TSV, embedded as a single step in `validate-github-metadata.yml`) is flagged out-of-family — its fixture tracking belongs under the **github-repository** skill charter. (3) [CI-032] Statement's cross-rule claim corrected: its pure-`uses:`-only routing carve-out NARROWS [CI-080]'s (additionally requires absence of `runs-on:`, confirmed in `validate-visibility-gate.py`'s docstring), not a mirror — [CI-080]'s own carve-out (`validate-harden-runner.py`) checks only for the absence of `steps:`. (4) [CI-054] Note and [CI-002] Permitted-list bullet corrected on advisory-linter mechanics: only `swift-linter` (a real `steps:`-bearing job) carries `continue-on-error: true`; the six nested linters (`lint-yaml`/`broken-symlink`/`license-header`/`test-support-spine`/`api-breakage`/`pr-title`) are `uses:` reusable-workflow jobs that CANNOT carry `continue-on-error` per [CI-105] — their advisory status comes from exclusion from `ci-ok`'s `needs:` list, via an `advisory: bool` input, confirmed against `swift-institute/.github/.github/workflows/swift-ci.yml`. All four items Clarifying per [SKILL-LIFE-003] — doc-only corrections to cross-rule claims and counts; no Statement's substantive requirement changed.
# 2026-07-03: Phase-2 skill re-sync from `Audits/CI-REVIEW-2026-07-03-phase1-dossier.md` (findings F8/F9/F10/F24/F32/F33/F34) + two seat-confirmed corrections + a validator fixture-coverage note. All Clarifying/Additive per [SKILL-LIFE-003] (reality already shipped; doc synced to it — no Statement changed in a way that invalidates currently-compliant code). [CI-030]/[CI-059] self-firing annotations corrected DEFERRED (validate-thin-callers.yml is workflow_call-only — no push/PR trigger; weekly-sweep-only; manifest `self-firing: deferred`) (F8). [CI-059] sub-org validator-amendment aspirational parenthetical replaced with shipped-note (`d56e36a`, matching [CI-004a]) (F9). [CI-080]/[CI-082] synced to self-firing ACTIVE + baseline 0 (empirically re-confirmed 2026-07-03 with positive+negative fixture controls per seat, Task 2a); [CI-090]/[CI-097] synced to self-firing ACTIVE + baseline 0 (batch-fix `5161e33..74ae3d0`, the permission-floor sweep) (F10); [CI-032] already synced by the Slice-A `12c9d5a` aggregation fix — left as-is. [CI-044] rationale de-staled — yq/lychee now cached (F24). Dead `swift-ci.yml:<line>` coordinates removed from [CI-011]/[CI-012]/[CI-013]/[CI-044]/[CI-054] (values retained; coordinates were drift-dead after swift-ci.yml grew to ~802 lines) (F32). [CI-013] Statement + enforcement SwiftyLab ref-shape de-staled — `SwiftyLab/setup-swift@v1` → version-agnostic `SwiftyLab/setup-swift` (SHA-pinned per [CI-107]), reflecting the Slice-C digest-pin; no literal SHA embedded, the F32 coordinate-drift lesson applied to action SHAs (F-followup). [CI-002]/[CI-054] advisory-linter soak enumerated alongside the gating format/lint pair (F33). [CI-058] target enumeration de-staled — swift-standards/.github + swift-foundations/.github wrappers also declare enable-private-repos (verified); validator coverage-gap noted (F34). [CI-095] dispatch-surface rider added — the private-skip gate evaluates against the triggering repo, so the centrally-dispatched ci-dispatch.yml (public hub, added 2026-07-03) is not private-skipped and a private target fails at checkout instead; cross-refs [CI-032]/[CI-096]. [CI-110] validator fail-fixture backfill provenance note — 2 of 4 fixtured this arc (`docc-structure` doc-020, `readme` readme-017); `github-metadata` + `package-structure` remain a residual harness-coverage gap (don't fit run.sh's 2-arg dir-scan contract), non-inertness confirmed out-of-band (seat positive-controlled package-structure; github-metadata is loud-error + CI-run), tracked as a successor run.sh-extension item. Provenance: `Audits/CI-REVIEW-2026-07-03-phase1-dossier.md`.
# 2026-07-03: [CI-107] clarifying+additive amendment — digest-pinning at latest (full commit SHA + `# vX.Y.Z` version comment) declared a permitted form for action refs and the PREFERRED form for credential-minting / privileged / security-sensitive actions; floating major tags remain permitted for benign refs (NO universal digest-pin mandate — the `actions/checkout@v6` floating sites are not newly flagged). Provenance: `actions/create-github-app-token` (35 sites — 26 in `.github/workflows/*.yml` + 9 in `.github/actions/configure-private-repos/action.yml`, each handed the bot App private key; the 26th is the arc's own `startup-failure-probe-weekly.yml`, `881696f`; recounted 2026-07-05 against swift-institute/.github @ 560a8da) + `SwiftyLab/setup-swift` (1 site — the merge-gating Windows job) digest-pinned to v3.2.0 (`bcd2ba4…`) / v1.14.0 (`38f54a7…`) to close a floating-tag supply-chain exposure on credential-minting / toolchain actions. Ratified NOT a [CI-107] violation (digest-pin-at-latest satisfies the latest floor and composes with [CI-082] re-lock-on-bump). Clarifying+Additive per [SKILL-LIFE-003].
# 2026-07-02: R3c reflections-drain fold — [CI-110] workflow-grep discipline note: matching Actions expression literals (`${{ … }}`) MUST use `grep -F` (BSD grep parses `{`/`$` as pattern syntax and silently returns 0 matches); pairs with [CI-110]'s diagnosis signature as the workflow-audit tooling-trap cluster. The Actions-enabled pre-flip vet landed as release-readiness [RELEASE-017] (merged per the 2026-06-26 triage), not here. Clarifying per [SKILL-LIFE-003]. Provenance: Reflections/2026-06-04-porter-ci-private-dep-access-arc.md.
# 2026-06-04 (second window): NEW [CI-110] mixed-trigger typed-boolean null-coercion (additive per [SKILL-LIFE-003]; the validate-* startup_failure class, 18 callers, fix 105b3b5; diagnosis signature failure+njobs=0+no-logs). [CI-004a] v2 sub-org sentence aligned to the executed pattern (ii) routing (clarifying per [SKILL-LIFE-003]: "not served by layer wrappers" → "not served via inherit; routed via parent wrapper + explicit-forward block"; validator d56e36a; sub-org wave 102/104 2026-06-04).
# 2026-06-04: α arc (HANDOFF-ci-private-dep-access) — NEW [CI-109] Cross-Org Secret Transport (explicit-forward at org boundaries; `secrets: inherit` is same-org-only; empirical: byte-primitives run 26959288611 "Configured 0" cross-org-inherit vs "Configured 4" same-org-inherit→cross-org-explicit, paired in one run). Additive per [SKILL-LIFE-003]. [CI-004a] v1→v2 BREAKING per [SKILL-LIFE-003] (principal-ruled α 2026-06-04 — the ruling is the discussion record): docs wrappers flip from not-required to REQUIRED for secret transport; consumers MUST route docs via their layer wrapper. [CI-059] sub-org caveat BREAKING for the sub-org caller class (principal-confirmed pattern (ii)): sub-org thin callers explicit-forward the named credential set; validate-thin-callers.py amendment queued (aspirational citation per [SKILL-LIFE-027]). Provenance: runs 26959288611/26959292363; commits 2930ed0 / ba0a229 / 311b074 / a1f4b58 / 033a862 / 81f239d.
# 2026-05-14: Pilot 16 of `/promote-rule` — mechanized `[CI-042]` (no `restore-keys` partial-prefix matching) by extending pilot 14's `validate-cache-policy.py` (12-line addition + aggregate regex broadening from `CI-040` to `CI-04[02]`). Canonical "compose-in-script" pattern — same workflow gates both rules. Synthetic fixtures: 8/8 pass (2 pass + 4 fail + 2 edge — re-classified `multiline-restore-keys` and `multi-cache-mixed` from edge to fail mid-implementation when run.sh surfaced the misclassification). Baseline: 0/0 across all 4 wrapper-host repos; self-firing already ACTIVE (inherited from pilot 14). New methodology lesson: "prefer existing script when the rule composes with an already-mechanized sibling — script-level analog of AST-linter 'prefer existing pack'". Provenance: `Audits/PROMOTE-CI-042-2026-05-14.md`. Clarifying per [SKILL-LIFE-003].
# 2026-05-14: Pilots 13/14/15 of `/promote-rule` — three workflow-validator MECHANIZE pilots dispatched from pilot 12's Class D queue. Pilot 13 mechanized `[CI-032]` (Public/Private Visibility Gate) via new `validate-visibility-gate.py` + `.yml` — single-repo multi-file integrity check sub-shape (third sub-shape, distinct from CI-010 single-target and validate-thin-callers per-package iteration). Baseline: 11 findings on `swift-institute/.github` orchestrator-only-callable reusables; self-firing triggers deferred until batch-fix lands. Pilot 14 mechanized `[CI-040]` (No `.build/` Cache, Permanent) via `validate-cache-policy.py` + `.yml`. Baseline: 0 findings across 4 wrapper-host repos; self-firing triggers ACTIVE immediately. Pilot 15 mechanized `[CI-080]` (Harden-Runner Audit-Mode Floor) via `validate-harden-runner.py` + `.yml`. Baseline: 3 findings on weekly-orchestrator `config` jobs; self-firing deferred. New methodology lessons: (a) baseline-clean vs baseline-violating pilots require different Phase-7 shapes (immediate gating vs deferred-trigger-activation); (b) single-repo multi-file integrity check is the third workflow-validator sub-shape — three canonical pilots now exist (CI-032/CI-040/CI-080); (c) aggregator-job carve-outs are name-based not structural (pragmatic compromise). Two pending USER-AUTHORIZED batch-fix arcs: CI-032 (11 jobs across 9 files), CI-080 (3 jobs across 3 files). Provenance: `Audits/PROMOTE-CI-032-2026-05-14.md`, `Audits/PROMOTE-CI-040-2026-05-14.md`, `Audits/PROMOTE-CI-080-2026-05-14.md`. Clarifying per [SKILL-LIFE-003].
# 2026-05-14: Pilot 12 sweep of `/promote-rule` — corpus-wide enforcement audit of all 55 [CI-*] rules. Triggered by principal redirect on `[CI-011]` ("can just be removed considering centralized + hierarchical CI/CD"). 6-class triage (Validator-enforced-annotate / Architecturally-enforced / Governance-only / MECHANIZE / TEXT-ONLY-permanent / TEXT-ONLY-deferred); 11 rules already enforced but missing the `Lint enforcement:` annotation (`[CI-010]` was pre-existing; 10 added this sweep on `[CI-001]`, `[CI-004]`, `[CI-020]`, `[CI-022]`, `[CI-031]`, `[CI-041]`, `[CI-043]`, `[CI-054]`, `[CI-060]`, `[CI-093]`); 7 rules architecturally subsumed by `[CI-031]` + canonical-reusable hardcoded values + branch-protection chain — Path-2 DOWNGRADE applied with `Lint enforcement (architectural)` annotation (`[CI-003]`, `[CI-011]`, `[CI-012]`, `[CI-013]`, `[CI-021]`, `[CI-053]`, `[CI-081]`). Total: 17 rule bodies amended. No Statement changes. Three methodology lessons for `lint-rule-promotion` next calibration: (a) architectural-enforcement is a third enforcement class missed by today's Phase 1 pre-check; (b) pause-surface MUST include rule body verbatim; (c) piggyback annotations on per-pilot outcome records prevent annotation-lag. Provenance: `Audits/PROMOTE-ci-corpus-sweep-2026-05-14.md`. Clarifying per [SKILL-LIFE-003].
# 2026-05-14: Pilot 10 of `/promote-rule` mechanized `[CI-010]` — new validator `validate-ci-matrix.py` + reusable workflow `validate-ci-matrix.yml` under `swift-institute/.github/.github/`. Verifies the universal `swift-ci.yml`'s 4-job matrix (`macos-release` / `linux-release` / `linux-nightly` / `windows-release`) per shape requirements (runner class + nightly's `continue-on-error: true`). New "centralized-config integrity check" sub-shape of workflow-validator path: targets a single file in a single repo (the `.github` org-profile repo) rather than iterating per-package. Validation: 0 findings against current `swift-institute/.github/.github/workflows/swift-ci.yml`; 5/5 synthetic-fixture scenarios pass (pass/dot-github-canonical, fail/missing-windows, fail/nightly-not-tolerant, fail/wrong-runner, edge/no-swift-ci). Clarifying per [SKILL-LIFE-003].
# 2026-05-10: Phase 3b cross-ref repair — removed broken [CI-005] citation in [CI-108]; replaced [CI-026] in [CI-106] body with [CI-090] (the live caller-side trigger-shape rule). Per [SKILL-LIFE-001].
# 2026-05-10: [CI-096] through [CI-107] added — bulk promotion of 12 CI/lint/GitHub feedback memories per agent-harness-engineering-comparative-analysis.md Wave 5 (memory→skill refactor). Source memories: feedback_ci_priority_axes, feedback_top_level_permissions_on_reusables, feedback_no_gh_cli_admin_scope, feedback_windows_first_class_ci_gating, feedback_no_toggle_bool_rule, feedback_no_regex_evasion_use_disable_with_reason, feedback_composite_action_description_no_expressions, feedback_env_invalid_in_runs_on_container, feedback_scheduled_workflow_canary_gap, feedback_continue_on_error_invalid_on_uses, feedback_workflow_call_permissions_chain, feedback_latest_versions_only.
```
