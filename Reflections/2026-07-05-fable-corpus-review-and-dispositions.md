# 2026-07-05 — Full-corpus Fable review, disposition waves, and store triage

**Context**: Fresh Fable seat booted from `BRIEF-fable-corpus-review.md` (meta-ecosystem seat's charter) the same day five skill monoliths were split and the memory corpus was dissolved to zero. Mandates: internal consistency, improvements, suboptimal rules, velocity drag, lint-coverage gaps, routing correctness. Terminal record: `swift-institute/Audits/REPORT-corpus-review.md` (findings, dispositions, §8 rulings, §8a/§9a execution).

**What happened**: 7 mechanical sweeps (cross-refs, stale paths, contradictions, description gates, hub completeness, drag evidence, lint gaps) + in-loop reading; every fatal-candidate sample-verified. 8 fatal-class contradictions found and fixed via 12 exclusive-ownership drafters + one serialized committer (~40 commits across Skills/Workspace/rule-institute/Engagement/.github; Research local-only). Principal ruled on all 8 queued decisions same-day; all executed, including the swift-kernel-primitives phantom sweep (~20 sites) and the scoped handoff-store triage (106→75 live, 30 evidence-retired, 4 records→Audits, ADT files frozen throughout).

**Patterns worth keeping**:
1. **Reversals rot the corpus through their citing sites, not their definitions.** Five of eight fatal contradictions were un-propagated 2026-05→07 reversals. Fixed structurally: [SKILL-LIFE-007] now covers REVERSE/Breaking-amend. Watch the next few breaking edits actually run the propagation grep.
2. **The author is structurally the wrong reviewer** — validated. A same-day fresh seat found index rot (readme's six-row hook-shift), a six-ID collision (RES-020–025), and an inverted checklist ([MEM-SAFE-024]) that per-skill self-review had passed over repeatedly.
3. **Sweep agents' verdicts need seat adjudication, not trust**: three sweep claims were overturned on sample-verification (a "missing" experiment that existed nested; phantom ledger rows one sweep took at face value; a "no size gate exists" claim). Verify-by-sample caught all three cheaply.
4. **Evidence-based store triage works and is cheap**: 3 agents × ~30 files with a strict probe rubric (in-file markers, artifact existence, git log) + seat overrides on class grounds cleared 34 files in one pass with zero guesswork retained (UNSURE → keep).
5. **Line-count ceilings are gameable by long lines** (ci-cd hub frontmatter) and depth-2 scans are a standing scope assumption — both documented, both currently harmless.

**Surfaced-but-parallel**: `_index.json` in this repo carries another session's uncommitted edits — this entry deliberately does NOT update the index ([REFL-016] leave-in-tree branch; [SUPER-056] no touching parallel WIP). Index update pending at that session's close.

**Action items**: none new — every follow-up is queued in `Workspace/BACKLOG.md` with triggers (tower-cluster store drain, L1 promote batch, [SKILL-LIFE-005] mechanization, [API-IMPL-008] linter fold, kernel validator residuals).
